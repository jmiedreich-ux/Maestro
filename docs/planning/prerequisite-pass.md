# Prerequisite pass for the verified development environment

Status: **observed on the Linux host on 2026-09-23; accepted by the Owner on 2026-09-24.** The feature code, including the automatic upgrade, is on `master`. Baseline runs used revision `827740cef6b16561bd582852f4ed1fa6e24b3b7b`; the post-install smoke used `ebbb1a1ebac497d2025c9ba888a169478b828160`. Use the [single environment contract](../development-process/environment-contract.md) as the full check list.

| Assumption or category | Observed result | Status |
|---|---|---|
| Actual Linux host and tools | Linux 7.0.0-30-generic; Git 2.43.0; Python 3.12.3; Bubblewrap present; 1671 GiB free. Baseline runner is Python `unittest`; on `master` 128 tests pass (agents 50, foundation 7, service 49 with 1 skipped, terminal 22). | Proved |
| Repository and clean workspace | The workspace manager created a fresh checkout at the pinned revision for every run. Every agent read its input, wrote its output and left source, input and assignment unchanged. Run data was removed and a second run repeated from a clean start, for all three routes. | Proved |
| Owner/service credentials | The service credential files exist; contents were never printed. As `jeremy` the service-owned files and `/etc/maestro/agents.toml` are unreadable, and the aggregate preflight reports that together with any other missing item. As the service user it passes. | Proved |
| Claude OAuth | The installed service credential expired on 2026-09-19. On 2026-09-23 it was replaced with a long-lived setup token in the existing credentials-file profile, with no code change. Claude 2.1.270 then ran on `claude-opus-4-6`. The token's real lifetime has not yet been observed; the expired file is kept as a protected backup. | Proved for launch |
| `agents.toml` and selected model route | The installed configuration has three routes: Codex 0.155.0 with `gpt-5.6-sol`, Claude 2.1.270 with `claude-opus-4-6`, and local Qwen Code 0.22.3 with `qwen3.6:27b` (added 2026-09-23). The Qwen route runs Node 22.23.2 with the Qwen Code package beside it under `/opt/maestro/tools/qwen`, and reads its model server profile from the service home. Tool-reported identity was observed in each run. | Proved |
| GitHub access | The Maestro Coordinator app installation has Administration: write (repository selection: all). A real write to the private QA repository (create then delete a scratch branch) succeeded and left nothing behind. | Proved |
| Mounts, permissions and egress | Each launch ran through the installed workspace isolation and the root-supervised egress guard as a systemd unit. Codex reached `chatgpt.com:443`, Claude `api.anthropic.com:443`, Qwen only the local model server at `127.0.0.1:11434`. | Proved |
| Service installation, ports and store | The installed service was upgraded to the `master` code, then checked ready and restarted. An authenticated read of the workspace returned 200 and an unauthenticated read was refused with 401. | Proved |
| Install/upgrade and rollback | The automatic upgrade (`deploy/upgrade.py`) was exercised on the installed service. A revision with no passed tag or not on `master` is refused. A forced failing post-install check restored the previous installation automatically, and the service stayed healthy (authenticated read 200, unauthenticated 401). An upgrade to the passed revision `bee22c9ddbca69d7d7ac8d8ccc74558dfd1573ba` then installed exactly that revision, with `agents.toml` and the Owner credential unchanged and a receipt and backup kept under `/var/lib/maestro/upgrades/`. The upgrade also passed isolated-tree tests covering success, failed check, failed install, refusal and waiting for a slow start. The timer `maestro-upgrade.timer` is enabled and installed a newly tagged revision by itself: the tag was pushed at 22:42:45 and the exact revision was installed at 22:43:45 on 2026-09-23, with the service healthy afterwards. | Proved |
| Data migration and reset | The workspace manager now resets a run, including read-only and agent-locked entries, and the egress guard hands a finished run's writable folders back to the service. The service user removed real Claude, Codex and Qwen runs with no elevated rights. No service schema is involved. | Proved |
| Logs, smoke and failure report | Two real runs per route on the baseline revision, then one real run per route through the installed package, launcher and configuration on the promoted revision; all exited 0, the agent's output matched a random input value and the pinned revision, and the harness baseline check passed. With the Qwen runtime removed in a test configuration the preflight named it, dispatch was refused, and no workspace was created; restoring it passed again. | Proved |
| Existing runner and Slack progress route | A host-local update loop (user service `maestro-m3-heartbeat`, reads a checkpoint file, posts to a Slack webhook) exists and delivered receipts were logged. The autopilot script exists but was not running. The repository's `LocalDurable` notification and the older `development-manager-loop` were not used as evidence. | Proved for progress updates; runner not verified |

Implementation owner: Claude Code.
