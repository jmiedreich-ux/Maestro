# Prerequisite pass for the verified development environment

Status: **observed on the Linux host on 2026-09-23; outcome not accepted.** Source pinned at `master` revision `827740cef6b16561bd582852f4ed1fa6e24b3b7b`. Code for the aggregate preflight and the local Qwen route is on the local branch `feature/verify-development-environment` (commits `e68cbcc`, `16764be`) and has not been reviewed or integrated. Use the [single environment contract](../development-process/environment-contract.md) as the full check list. Evidence reviewer: Codex.

| Assumption or category | Observed result | Status |
|---|---|---|
| Actual Linux host and tools | Linux 7.0.0-30-generic; Git 2.43.0; Python 3.12.3; Bubblewrap present; 1671 GiB free. Baseline runner is Python `unittest`: 116 tests passed on the pinned revision (agents 38, foundation 7, service 49 with 1 skipped, terminal 22). | Proved |
| Repository and clean workspace | The existing workspace manager created a fresh checkout at the pinned revision for every run. Every agent read its input, wrote its output and left source, input and assignment unchanged. The run data was removed and a second run repeated from a clean start, for all three routes. | Proved |
| Owner/service credentials | The service credential files exist; contents were never printed. As `jeremy`, the service-owned credential files and `/etc/maestro/agents.toml` are unreadable, so the aggregate preflight reports them together with the other missing items. As the service user it passes. | Proved |
| Claude OAuth | The installed service credential had expired on 2026-09-19 (refresh token present). It was replaced on 2026-09-23 with a long-lived setup token stored in the existing credentials-file profile; no code change was needed. The installed Claude 2.1.270 then answered on `claude-opus-4-6`. The token's real lifetime is unverified; the expired file is kept as a protected backup. | Proved for launch; lifetime unverified |
| `agents.toml` and selected model route | Installed routes parse. Codex 0.155.0 with `gpt-5.6-sol`, Claude 2.1.270 with `claude-opus-4-6`. Tool-reported identity was observed in each run (Codex header, Claude model usage). Local Qwen 0.22.3 with `qwen3.6:27b` is a third route in a verification configuration; the installed configuration is unchanged because the installed service does not include the Qwen route change. | Proved |
| GitHub access | The Maestro Coordinator app installation has Administration: write (repository selection: all), so the reported missing permission is not the case. A real write to the private QA repository (create then delete a scratch branch) succeeded and left nothing behind on 2026-09-23. | Proved |
| Mounts, permissions and egress | Each launch ran through the installed workspace isolation and the root-supervised egress guard as a systemd unit. Codex reached `chatgpt.com:443`, Claude `api.anthropic.com:443`, Qwen only the local model server at `127.0.0.1:11434`. | Proved |
| Service installation, ports and store | The installed service is running but not required for the baseline agent run. | Excluded with reason |
| Install/upgrade and rollback | Tool versions recorded above. The installed service and launcher were not upgraded; a verification-only launcher and configuration were added beside them. | Excluded for service upgrade |
| Data migration and reset | Run-owned data reset proved. Agent-written files in a run's home are not removable by the service user, so reset needs elevated removal; recorded as a gap. No service schema is involved. | Proved; gap recorded |
| Logs, smoke and failure report | Two real runs per route, all exit 0, with the agent's output checked against a random input value and the pinned revision, and the harness baseline check passing. With the Qwen runtime removed in a test configuration, the preflight named it, dispatch was refused, and no workspace was created; restoring it passed again. | Proved |
| Existing runner and Slack progress route | A host-local update loop (user service `maestro-m3-heartbeat`, reads a checkpoint file, posts to a Slack webhook) exists and delivered receipts were logged. The autopilot script exists but was not running. The repository's `LocalDurable` notification and the older `development-manager-loop` were not used as evidence. | Proved for progress updates; runner not verified |

Implementation owner: Claude Code. Open: the elevated-reset fix, adding the Qwen route to the installed service, and the Claude setup token's lifetime.
