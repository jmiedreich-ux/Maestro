# Independently reviewed packet delivery — milestone QA plan

Version 1. Manual acceptance procedure specification. Incorporates all [shared QA rules](README.md), including authority, resource binding, evidence, cleanup and the runtime conversion checklist. No checks have run.

## Milestone and controlling outcomes

Applies only to [Independently reviewed packet delivery](../development-milestones.md#independently-reviewed-packet-delivery). Its allocated packets, integration dependencies and linked pinned project declarations define scope. Read their full acceptance criteria and definitions of done; these concrete scenarios may share evidence but do not waive an unlisted required criterion. Product behavior remains in [architecture](../../../architecture.md#milestone-quality-assurance-and-test-data).

## Entry and setup

Previous milestones accepted and every packet in this development milestone integrated, including pause/stop and architectural determinations. This test exercises the installed Execution engine on a disposable sample, not authorization to develop Maestro.

Execute the shared setup sequence and record the exact installed candidate, plan/input hashes and resolved resources before the checks. Resource names and script hashes are unresolved implementation/binding inputs, not fabricated values. No new account, host, repository or credential is provisioned by this plan.

## Test inputs and real entry paths

A real confirmed sample breakdown with two independent small coding tasks, a declared shared boundary and one legitimate question. Use actual local Qwen and required configured cloud path; separate read-only reviewer session. Generated source inputs cannot contain fabricated reviews or results.

Record actual input hashes/lineage and sanitization classification under the shared data rules. Use the real public path named in each check; capture failures as well as successes. Data preparation must not insert the expected result into internal state.

## Required checks

| Check | Procedure | Expected result / essential failure boundary | Required evidence |
|---|---|---|---|
| Start and saved configuration | Start eligible sample Execution through CLI with collected Development Manager selection; replay start and submit an ineligible/stale request. | Exactly one activity binds confirmed registration/breakdown, code baseline and separately observed product-master head. Viewing or architecture completion alone does not dispatch. | Start receipt, snapshot, exact references and rejection evidence. |
| Manager plan and reservation | Observe actual persistent manager planning independent tasks, responding to a linked question and reconsidering events. | Service enforces dependencies, path/resource conflicts, route capability/location/context/capacity and exact model before reservation. No polling model loop or silent route substitution. | Manager response/reason, question receipt, reservation and rejected conflict/mismatch. |
| Coder plan and actual delivery | Run actual local Qwen and a justified configured cloud task; inspect visible implementation plan before continued work and resulting scoped commits. | Plan creates no new approval gate; actual changed paths/checks/result and clean local head match service-verified remote revision. | Actual tools/model evidence, plan, changed-path list, checks, Git journal and remote commit. |
| Independent semantic review | Review exact delivered revision in separate non-author workspace, then complete a valid correction if found. | Real semantic judgment, not mechanical approval; approval applies only to exact revision and means integration eligibility, not milestone acceptance. | Reviewer identity/input hash, findings, original-author correction and remaining count. |
| Architect determination and limit | Exercise an actual ambiguous in-scope question and configured review-limit case using the real determination/typed decision path. | Architect gives bounded recommendation, not approval/dispatch; unauthorized/replayed grant is rejected, changed scope is not silently approved. | Determination inputs/output, linked decision and durable allowance receipts. |
| Pause, resume and stop | Pause during actual coder/review work, try outside-set reservations, resume eligible work and gracefully stop a separate active run. | Recorded settlement set controls downstream work; new outside work is refused; unknown stop remains uncertain, unfinished work is not completed. | Before/after set, live process/run state, refusals and stopped/paused CLI state. |
| Restart and monitoring | Restart with pending delivery/review, reconnect CLI and observe missing/stale runtime measurements. | Same identities, grants, deadlines and counts survive; no duplicate publication/assignment, mutation replay or fabricated zero measurement. | Process/journal reconciliation, exact counters, CLI/API/events and checkpoints. |

## Completion and correction

Every required check must satisfy the shared PASS boundary on the same exact candidate. A required missing path, missing tool, missing observation or unavailable artifact is UNTESTED and blocks acceptance; an observed behavior defect is FAIL. Use existing correction and affected-rerun rules, not an extra QA approval round. Independent whole-milestone review remains separate and required before promotion.

Accept independently reviewed packet delivery and its lifecycle controls only. Integration, final QA, promotion and completed Execution require their later real paths.

## Cleanup and reset

Settle sample Execution according to recorded disposition before shutting down; retain scoped branches/results/reviews and unfinished-work evidence. Do not claim sample milestone promotion, which is not delivered by this group.

Apply the shared reset checklist. Capture evidence before cleanup, record retained/deferred resources and quarantine unsafe or uncertain environments. Do not destroy evidence or widen cleanup scope to obtain a clean result.
