# Verified milestone completion — milestone QA plan

Version 1. Manual acceptance procedure specification. Incorporates all [shared QA rules](README.md), including authority, resource binding, evidence, cleanup and the runtime conversion checklist. No checks have run.

## Milestone and controlling outcomes

Applies only to [Verified milestone completion](../development-milestones.md#verified-milestone-completion). Its allocated packets, integration dependencies and linked pinned project declarations define scope. Read their full acceptance criteria and definitions of done; these concrete scenarios may share evidence but do not waive an unlisted required criterion. Product behavior remains in [architecture](../../../architecture.md#milestone-quality-assurance-and-test-data).

## Entry and setup

All previous milestones accepted; all seven final-group packets integrated. Actual catalog-bound plans, isolated setup, artifact storage, QA/reviewer routes and approved test-repository promotion target are required. The candidate under test is Maestro; sample product master is only in the QA repository.

Execute the shared setup sequence and record the exact installed candidate, plan/input hashes and resolved resources before the checks. Resource names and script hashes are unresolved implementation/binding inputs, not fabricated values. No new account, host, repository or credential is provisioned by this plan.

## Test inputs and real entry paths

A disposable sample project registered and architecturally confirmed through real earlier paths, with two dependency-linked milestones, small real coding changes and a genuine in-scope gap. Run-owned generated inputs exercise real agents, review, Git, QA and completion. Required denial cases use a revoked/mismatched test binding and removed test evidence under controlled, scoped authorization.

Record actual input hashes/lineage and sanitization classification under the shared data rules. Use the real public path named in each check; capture failures as well as successes. Data preparation must not insert the expected result into internal state.

## Required checks

| Check | Procedure | Expected result / essential failure boundary | Required evidence |
|---|---|---|---|
| Bind and set up | Select actual catalog names/hash in sample plan, start QA from exact sample milestone candidate and verify resolved test credentials/configuration; try revoked/changed/production binding. | Only authorized test setup starts; provenance, identities, ports and health are saved. Revoked/missing/changed binding is UNTESTED with no substitution. | Catalog/plan/config hashes, non-secret credential versions, setup/process/health and denial records. |
| Real checks and unavailable path | Exercise sample user journey with generated input through real product entry; withhold one required prerequisite in a separate run. | Expected real result produces PASS only with evidence. Missing/bypassed path is UNTESTED, visibly blocks promotion and is not called defect or success. | Data lineage, actual input/result path, expected/observed results and CLI blocker. |
| Artifact integrity and reset | Capture/read actual artifacts, test hash-mismatch or missing evidence before closure, and fail a controlled shutdown/reset. | Identity/hash/size/media type and authorized access work; missing evidence invalidates check to UNTESTED; failed cleanup quarantines without changing prior result. | Artifact read/hash, invalidation, redaction/retention and quarantine/reset evidence. |
| Gap and bounded correction | Have actual QA/outcome review identify the genuine gap; run read-only milestone-gap determination, bounded supplement where needed, implementation/review/integration and affected rerun. | Defect routes to Integration, missing in-scope work to valid immutable supplement, scope change to Owner disposition. No counterfeit findings or counters; unrelated valid evidence retained. | Finding-to-determination links, supplement publication/activation and corrected capability evidence. |
| Independent milestone review and promotion | Use fresh non-author/non-integrator outcome review on exact assembled sample milestone and QA evidence; attempt promotion with missing gate/stale target, then promote eligible revision. | Both existing gates and promotion dependencies are enforced. Eligible non-fast-forward merge is remotely verified without a new Owner approval; wrong target/gates block. | Review and QA references, rejected request and actual sample repository merge graph. |
| Completed-provider delivery | After real provider promotion/completion, deliver exact passing provider head to consumer, including a prior excess-closure wait and an already-contained case. | Verified completion record/head/closure binds FIFO delivery or idempotent containment; moving master and readiness flags cannot release work. | Actual provider completion, tested head/merge containment, consumer import and mismatch rejection. |
| Recovery and stopped closure | Interrupt actual QA/promotion/completion publication, restart and reconcile; run pause/stop across downstream stages and then explicit idle-only re-registration/replanning. | Saved restrictions, identities, budgets, artifacts and journals survive; uncertain effects block advance; stopped work lists unfinished items, not successful completion. Later steps start only explicitly. | Reconciled operations, stop record, exact before/after refs and real follow-on process evidence. |
| Final acceptance and automatic completion | Re-exercise earlier connected milestone journeys on this final candidate, map all 18 registered outcome criteria to current evidence, then allow sample Execution closure. | All required sample milestones are promoted, corrections/active work/uncertainty resolved and completion bytes verified before SQL/CLI success. No completion from packet counts alone. | Full outcome matrix, real regression artifacts, remote completion hashes and correlated SQL/CLI notification. |
| Retention and cleanup | Close the sample Execution and observe configured artifact-retention scheduling using an explicitly controlled clock if needed; complete scoped cleanup and reset. | Evidence persists through closure plus configured retention; scheduled deletion leaves identity/hash/reason tombstone. Cleanup never deletes unrelated data or rewrites QA outcome. | Retention configuration/clock limitation, tombstone, cleanup manifest and reset result. |

## Completion and correction

Every required check must satisfy the shared PASS boundary on the same exact candidate. A required missing path, missing tool, missing observation or unavailable artifact is UNTESTED and blocks acceptance; an observed behavior defect is FAIL. Use existing correction and affected-rerun rules, not an extra QA approval round. Independent whole-milestone review remains separate and required before promotion.

This is the final connected reconciliation, not a substitute for earlier checks. Every criterion in all five pinned declarations must link to a passing real observation or remain explicitly unmet; a plan row or a source-code inspection is not evidence.

## Cleanup and reset

Retain final outcome evidence, promotion/completion refs and required artifacts for configured lifetime; stop only owned processes and quarantine failed resets. Remote test branches remain unless their exact deletion is separately authorized.

Apply the shared reset checklist. Capture evidence before cleanup, record retained/deferred resources and quarantine unsafe or uncertain environments. Do not destroy evidence or widen cleanup scope to obtain a clean result.
