# Confirmed development planning — milestone QA plan

Version 1. Manual acceptance procedure specification. Incorporates all [shared QA rules](README.md), including authority, resource binding, evidence, cleanup and the runtime conversion checklist. No checks have run.

## Milestone and controlling outcomes

Applies only to [Confirmed development planning](../development-milestones.md#confirmed-development-planning). Its allocated packets, integration dependencies and linked pinned project declarations define scope. Read their full acceptance criteria and definitions of done; these concrete scenarios may share evidence but do not waive an unlisted required criterion. Product behavior remains in [architecture](../../../architecture.md#milestone-quality-assurance-and-test-data).

## Entry and setup

Previous milestones accepted; QA resource catalog and all Architecture-group packets integrated. Exact architect/reviewer models are collected separately; catalog producer/schema extensions must exist for this test project, not be emulated with hand-inserted records.

Execute the shared setup sequence and record the exact installed candidate, plan/input hashes and resolved resources before the checks. Resource names and script hashes are unresolved implementation/binding inputs, not fabricated values. No new account, host, repository or credential is provisioned by this plan.

## Test inputs and real entry paths

Use a genuinely confirmed run-owned Registration project with small existing code, two independent source areas, one shared dependency and a real ambiguity to resolve. Provide actual non-secret test binding catalog through the implemented producer. Retain a later source/registration amendment.

Record actual input hashes/lineage and sanitization classification under the shared data rules. Use the real public path named in each check; capture failures as well as successes. Data preparation must not insert the expected result into internal state.

## Required checks

| Check | Procedure | Expected result / essential failure boundary | Required evidence |
|---|---|---|---|
| Explicit entry and view | View architecture, then explicitly start from current confirmed idle registration; repeat start and try invalid/unconfirmed/conflicting entry. | Viewing/Registration confirmation starts nothing. One reserved activity uses exact package-to-outcome mapping, while unrelated project remains available. | CLI requests, activity/reservation and verified source/outcome mapping. |
| Investigation and specialists | Let actual architect inspect code and publish foundations, source-local roles and contexts. | Findings cite source evidence and distinguish operation; structure/ownership and required role/context headings are correct; no implementation or specialist dispatch occurs. | Investigation/decision/structure records, source revision, role/context paths/hashes. |
| Questions and persistent continuation | Answer the genuine design ambiguity, interrupt/resume and exercise supported context continuation. | Same logical session or validated replacement retains findings, decisions, identity/versions and accounting; no unexplained repeated investigation. | Question receipts, checkpoints, session/model and before/after published records. |
| Breakdown and catalog validation | Produce bounded packets and milestone QA records through actual agents using the catalog; validate required execution requirements, QA references and complete inventory. | Schemas accept defined records and reject missing/stale/out-of-scope references, unknown catalog names/hash and unsafe overlapping paths; parallelism has real dependencies, not a fixed schedule. | Actual packet/milestone/QA records, catalog provenance/hash, validator results and inventory. |
| Review, amendment and confirmation | Independently review exact foundations/breakdown, apply legitimate corrections and confirm exact published eligible version through CLI. | Non-author inputs cover required content; counters remain bounded; stale expected version/hash rejects confirmation; accepted confirmation starts no Execution. | Reviews/changes, reviewed-content hash, remote manifest/receipt and resulting state. |
| Cancellation and restart | Cancel with actual activity/operations active, reconcile termination and start eligible continuation under existing rules. | Saved work and old confirmation remain; reservation lasts until uncertainty resolves; remaining budgets are retained, not reset. | Operation/run settlement, records and visible canceled/restarted state. |
| Changed registration | Perform actual confirmed re-registration with changed scope, then manually start architecture reconciliation. | Affected old breakdown becomes ineligible; unaffected identities/evidence persist, changed records version correctly; only explicit valid steps restore readiness. | Old/new mappings, invalidation closure, review references and reconciled confirmation. |
| Limits and catalog failures | Exercise one real configured review/timeout limit and one missing/changed test binding without fabricating a grant. | Typed eligible one-use action retains base allowances; missing resource or changed binding prevents affected plan confirmation and is visibly explained. | Actual limit/decision receipts and validation failure, without credential values. |

## Completion and correction

Every required check must satisfy the shared PASS boundary on the same exact candidate. A required missing path, missing tool, missing observation or unavailable artifact is UNTESTED and blocks acceptance; an observed behavior defect is FAIL. Use existing correction and affected-rerun rules, not an extra QA approval round. Independent whole-milestone review remains separate and required before promotion.

This verifies the product's Architecture process on a sample project; its test confirmation is not Owner confirmation of the Maestro manual development breakdown.

## Cleanup and reset

Retain foundations, reviews, confirmations and both registration histories. Settle active sessions/publications before shutdown; quarantine uncertain session/resource ownership.

Apply the shared reset checklist. Capture evidence before cleanup, record retained/deferred resources and quarantine unsafe or uncertain environments. Do not destroy evidence or widen cleanup scope to obtain a clean result.
