# Maestro manual architecture — code investigation

Status: source-supported preparation, version 1. No implementation or installed verification has been performed.

## Accepted inputs and authority

The Owner confirmed [manual registration candidate version 1](https://github.com/jmiedreich-ux/Maestro/blob/4953aa555b9e5da5b73407f62184c7905efb7898/docs/planning/manual-registration.md). The [actual receipt](../../../ai/handoffs/current.md#manual-registration-owner-confirmation) records the response and exact candidate hash. All five declarations and 18 outcomes remain included. Code and governing documents are pinned to source commit `182bf4290ef2960d4691a7f41315ebf71d5bf85c`.

This manual investigation supplies the evidence that a future runtime `investigation.json` would carry. It is not a service-generated architecture version, registration reference, SQL record or confirmed breakdown. Plain subjects identify the draft work; runtime identities will be assigned by the service if later imported. No invented allocation, catalog, credential, model selection or manifest is supplied.

## Inspection method and limits

The complete tracked repository tree and all 35 files under `services/` were fetched at the pinned source commit. Their Git blob identities were checked against the repository inventory. All Python top-level function/class names were indexed by static parsing; only the locations listed below were examined semantically. Fetching or indexing a file is not a full code audit.

No service, existing CLI, model, test, migration, Git operation from the inspected product, credential provider or external endpoint was executed. Installed software, database contents, secrets and repository permissions remain unknown. Old source comments and claims of passing tests are historical evidence, not current authority.

## Code-direction findings

Paths in this table are relative to `services/maestro/maestro/`. Each disposition is an architect decision for the draft breakdown, subject to its independent review and later exact Owner confirmation. It does not move, replace or delete code now.

| Area and examined location | Source-supported finding | Direction and necessary work |
|---|---|---|
| Package: `../pyproject.toml` | Python >=3.12, setuptools package, Alpha version, `maestro.cli:main`; declared PyYAML, PyJWT and cryptography dependencies | Retain Python/package basis. Revalidate and lock needed dependencies during implementation; add installed service/CLI entry points and deployable schema resources. Existing versions are observations, not compatibility recommendations. |
| Runtime paths: `config.py`, `validate_runtime_dir`, `RuntimeConfig`, descriptor helpers | Runtime is constrained to repository `var/`; symlink rejection and descriptor-based access are implemented | Amend path policy for the configured local service root and `/var/lib/maestro/maestro.sqlite3` default. Reuse safe-path techniques after focused verification; preserve rejection before mutation. Do not copy the Alpha-only root restriction into the new service. |
| Persistence: `storage.py`, connection/migration/health methods and schema constants; `operational_state.py`, states/routes, canonical JSON/digest and context validation | SQLite WAL, foreign keys, short immediate transactions, schema history and versioned state scaffolding exist. Tables and state routes encode the old packet/run workflow. The inspected connection setup does not explicitly configure FULL synchronous writes | Reuse connection/transaction/hash techniques selectively. Introduce the current service-owned store and domain repositories; explicitly enforce WAL/FULL, request receipts, start reservations, process states and atomic event publication. Do not equate old schema version 5 with the new process contracts. Existing database handling must reject unsupported stores without silently replacing them. |
| Source access: `git_repository.py`, complete `ReadOnlyGitRepository` | Exact-commit regular-blob reads, size bounds, symlink/submodule rejection and argument-array Git access exist | Reuse with bounded command handling and source/reference validation tests. Keep reads separate from credentialed publication. Exact reads do not prove branch authorization or remote publication. |
| Authority intake: `project_manifest.py`, parser/shape; `project_authority.py`, `ProjectAuthorityLoader.load`; `project_discovery.py`, `observe_repository`, `discover_project` | Old intake requires `maestro.project.yaml`, a work-graph path and historical routing fields; discovery guesses candidate document paths | Replace that intake orchestration with the overview's explicit references, scope, source and publication selection. Reuse bounded parsing/file-inventory techniques where compatible. Do not make the old YAML manifest a new registration prerequisite. |
| Registration: `project_onboarding.py`, `register_project`, `materialize_and_ready_packet`; `cli.py`, parser and registration dispatch | Registration activates a binding, records graph/work items, and creates a Planned run; packet materialization walks readiness states | Replace the registration workflow. Candidate assessment, independent review, publication and explicit confirmation must be separate; registration must not create implementation work or start Execution. Current graph/run orchestration is not an input adapter for the manual candidate. |
| API: `read_api.py`, configuration, command envelope, route tables and request dispatch | Default port 8765, optional non-loopback environment exception, snapshot/SSE routes and four older mutation handlers. Envelope accepts an actor object; inspected dispatch does not establish the new Owner-token boundary | Replace public routing/configuration with authenticated loopback `/api/v1`, default 8787, durable receipts and current projections. Reuse serialization/SSE framing concepts only after their cursor, authentication and reconnect behavior is checked. Remove the historical external-host exception from the new public path. |
| CLI: `cli.py`, `build_parser`, `main` | Alpha argparse commands invoke local storage/workflow functions; no current terminal workspace is established by these paths | Build the connected terminal as a service client. No direct database writes, no CLI-triggered service launch, no replay of uncertain mutations. Implement each process's commands with its owning delivery work. |
| Agent adapter: `executor.py`, complete local Qwen adapter and Git evidence helpers | Launches `qwen -p ... --yolo`; handles live in memory; preflight fields exist but submit only checks the worktree directory; result examines local HEAD | Replace supervision/authorization orchestration while retaining useful subprocess observation patterns. Exact model, capability, isolation, deadline, saved PID/start evidence, continuation and verified remote result belong to new shared adapters. A zero exit or local commit never supplies review approval. |
| Dispatch: `dispatch_orchestrator.py`, complete `run`; `development_manager.py`, dispatch pool and `run_cycle` | Threaded launch/heartbeat/finish primitives exist. Agent launch precedes the saved start call, and live handles are not themselves restart evidence | Build durable launch/reconciliation with shared service-owned operations and bounded technical recovery. Preserve separate agent work, process activity and persistent-session identity. No silent launch duplication after restart. |
| Review: `development_manager.py`, `progress_review`, `_approve_review`, `run_cycle`; `review_dispatch.py`, route signatures | Driver can record `Approve` mechanically after readiness checks; integration validation precedes this constructed independent approval | Replace review orchestration with actual separate non-author reviewer assignments and exact revision binding. Keep deterministic readiness and semantic review distinct. Implement original-author corrections and persistent process-specific counts. |
| Readiness: `review_readiness.py`, `evaluate_review_readiness` preflight and helper inventory | Structured candidate/check evidence and immutable-revision checks exist | Retain as a candidate for extracting deterministic checks, not as independent approval. Full check execution, launch-error handling and result ordering still need targeted implementation assessment; no blanket reuse assurance. |
| Acceptance/integration: `owner_acceptance.py`, `accept_packet`; `merge_executor.py`, complete `perform_merge`; `development_manager.py`, merge loop | Old acceptance is initial-attempt-specific; driver supplies Owner authority labels; merge executor checks out and locally merges into the supplied default branch | Replace authority and integration control. Deliver independent packet approval, project FIFO integration, milestone branches, declared dependency import, assembled QA/outcome gates, and journaled remote promotion. No per-packet Owner acceptance is added. Pure Git mechanics may be extracted after verification. |
| Recovery: `auto_redispatch.py`, complete function; old state route inventory | Recovery can close a packet and materialize a new identity with correction count zero | Replace this route for the new processes. Recovery/replacement must preserve current approved work, budgets and exact evidence; changed architecture follows re-registration and separately started architecture preparation. No retry allowance is created by renaming. |
| Credentials/GitHub: `secrets.py`, complete provider; `github_client.py`, callable inventory and `_request_json` | Protected local-file secret resolution exists; GitHub helper raises HTTP errors but inspected request helper has no explicit timeout | Reuse secret-reference and restrictive-file techniques after adaptation to service-owned credential paths. Add separately authorized repository profiles, bounded calls, operation journals and remote reconciliation. Agent access and service publication authority remain distinct. No secret files were inspected. |
| Synthetic path: `packet_contract.py`, `ApprovedPacket.from_mapping`; `packet_wrapper.py`, synthetic executor/wrapper; synthetic-discovery callable inventory | Old packet contract requires a synthetic executor/scenario, and the fixture worker constructs results without a model or subprocess | Exclude from the new production entry points. Retain current files until an approved implementation packet removes or isolates obsolete callers. New tests may generate inputs, but must exercise the real result-producing path. No deleted historical fixtures are restored as delivery proof. |

## Outcome consequences

Runtime, CLI, registration, architecture preparation and Execution need connected new implementations over selective utility reuse. This is not a wholesale rewrite mandate and does not mark any supplied project outcome complete.

The draft [work map](work-breakdown.md) assigns the changes and missing capabilities. [Project structure](project-structure.md) separates present files from intended destinations. [QA resources](qa-resources.md) records the unavailable operator inputs. The old workflow remains untouched until authorized implementation; old docstrings do not authorize its continuation.

## Evidence still required

Implementation must establish actual service installation, Owner authentication, current tables/schemas, real tool/model identity, source and publication access, restart behavior, real reviewer decisions and connected acceptance. No old test count or source comment supplies that evidence. The detailed development records and QA plans remain unfinished; these foundations are not an approved implementation input.
